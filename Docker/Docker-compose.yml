version: '3.8'

services:
  php-server:
    image: php:8.2-apache
    container_name: php-server
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    

      

      